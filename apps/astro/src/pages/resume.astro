---
import BaseLayout from "../layouts/BaseLayout.astro";
import { resumePageSchema } from "../schemas/pageSchemas";

const { pageTitle } = resumePageSchema.parse(Astro.locals.pageData);
---

<BaseLayout>
  <h1>{pageTitle}</h1>
</BaseLayout>
